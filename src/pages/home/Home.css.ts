import { style } from '@vanilla-extract/css';

export const main = style({
	flex: 2,
	width: '68vw',
	margin: '0 auto',
});

export const container = style({
	display: 'flex',
	flexWrap: 'wrap',
});

export const title = style({
	marginBottom: 8,
});

export const productsBox = style({
	marginBottom: 15,
});
